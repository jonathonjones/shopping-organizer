<h2>Sign in</h2>

<%= button_to "Sign in using Facebook", '/auth/facebook' %>

<% if Rails.env.test? || Rails.env.development? %>
  <% @users.each do |user| %>
    <%= button_to "Sign in as #{user.name}", sign_in_as_path(:id => user.id) %>
  <% end %>
<% end %>